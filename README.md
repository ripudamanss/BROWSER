# PyQt Browser 🧭

A lightweight desktop web browser built using **PyQt5**, **PyQt6** and **Qt WebEngine**.  
This project demonstrates how to create a tab-based browser with navigation controls, loading indicators, and basic browser settings using Python.

---

## 🚀 Features

- 🌐 Web browsing using **Qt WebEngine**
- 📑 Tab-based interface
- 🔍 URL/Search bar
- ⏳ Page load progress indicator
- 🧭 Back / Forward / Reload navigation
- 🧩 Context menus & dialogs
- ⚙️ Web settings control (JavaScript, images, etc.)
- 🎨 Clean PyQt UI layout
- 🔐 Uses `QWebEngineProfile` for better session handling

---

## 🛠 Tech Stack

- **Python 3.12 & above**
- **PyQt5**
- **PyQt6**
- **PyQt WebEngine**
- **Qt WebEngine (Chromium-based)**

---

## 📦 Requirements

Make sure Python is installed, then install dependencies:

```bash
pip install PyQt5 PyQtWebEngine PyQt6
```
👨‍💻 Author
-Built by Ripudaman
-AI & Engineering Student
